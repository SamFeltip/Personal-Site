---
type Props = {
  className?: string;
  delay?: number;
};

const { className = "", delay = 0 }: Props = Astro.props;
---

<article
  class={`py-5 max-w-none motion-preset-slide-up ${className}`}
  style={`--motion-delay: ${delay}ms`}
>
  <slot />
</article>
