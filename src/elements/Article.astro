---
type Props = {
  className?: string;
  delay?: number;
};

const { className = "", delay = 0 }: Props = Astro.props;
---

<article
  class={`py-5 max-w-none motion-safe:opacity-0 motion-safe:animate-show ${className}`}
  style={{ "animation-delay": `${delay}ms` }}
>
  <slot />
</article>
